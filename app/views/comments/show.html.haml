%h3 Show Comment

- if notice
  .alert.alert-success #{notice}

= render "links/link", link: @comment.link

.well
  %p= @comment.body

  .help-block
    Added by #{@comment.username} #{time_ago_in_words(@comment.created_at)} ago

  .actions
    = link_to 'Edit', edit_comment_path(@comment),
      class: "btn btn-default"
    = link_to 'Delete', @comment,
      method: :delete,
      data: { confirm: 'Are you sure?' },
      class: "btn btn-danger"
